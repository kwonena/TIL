### **문제 조건과 설명**

문자열 S를 입력받은 후에, 각 문자를 R번 반복해 새 문자열 P를 만든 후 출력하는 프로그램을 작성하시오. 즉, 첫 번째 문자를 R번 반복하고, 두 번째 문자를 R번 반복하는 식으로 P를 만들면 된다. S에는 QR Code "alphanumeric" 문자만 들어있다.

QR Code "alphanumeric" 문자는 0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ\\$%\*+-./: 이다.

---

### **Input**

첫째 줄에 테스트 케이스의 개수 T(1 ≤ T ≤ 1,000)가 주어진다. 각 테스트 케이스는 반복 횟수 R(1 ≤ R ≤ 8), 문자열 S가 공백으로 구분되어 주어진다. S의 길이는 적어도 1이며, 20글자를 넘지 않는다.

---

### **Output**

각 테스트 케이스에 대해 P를 출력한다.

---

### **스스로 생각하기**

첫째줄에 테스트 개수 t를 입력받습니다.

테스트의 개수만큼 반복문을 돌리며 반복 횟수 r과 문자열 s를 공백으로 구분해 차례대로 입력받습니다.

r은 정수로 저장되어야 하기 때문에 int형으로 변환해줍니다.

문자열 s의 길이만큼 반복문을 돌려주며 문자 하나씩 r만큼 곱해 출력해줍니다.

---

### **소스코드**

```
t = int(input())  # 테스트 개수

for i in range(t):
    r, s = input().split()  # r : 반복 횟수, s : 문자열
    r = int(r)

    for j in s:
        print(j * r, end="")  # j를 r만큼 반복해서 출력

    print("\n", end="")
```
